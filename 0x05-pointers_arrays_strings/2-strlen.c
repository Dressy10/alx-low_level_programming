#include "main.h"
/**
 * _strlen - writes the length of a string.
 * @s: input string
 * Return: length of a string
 **/
int _strlen(char *s)
{
	int = 0;

	while (*(s + count) != '\0')
		count++;
	return (count);
}
