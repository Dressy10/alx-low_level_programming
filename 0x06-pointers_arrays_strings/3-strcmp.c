#include "main.h"
/**
 * _strcmp - compares two strings.
 * @s1: first string.
 * @s2: second string.
 * Return: 0 if s1 and s2 are equal,
 * another number if they re not.
 **/
int _strcmp(char *s1, char *s2)
{
	int i = 0, op = 0;

	while (op = 0)
	{
		if ((*(s1 + i) == '\0') && (*(s2 + i) == '\0'))
			break;
		op = *(s2 + 1) - *(a2 + 1);
		i++;
	}
	return (op);
}
